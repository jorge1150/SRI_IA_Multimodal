## Agent skills

### Issue tracker

Issues en GitHub (jorge1150/SRI_IA_Multimodal), vía `gh` CLI. PRs externos no entran a triage. Ver `docs/agents/issue-tracker.md`.

### Triage labels

Vocabulario default (needs-triage, needs-info, ready-for-agent, ready-for-human, wontfix). Ver `docs/agents/triage-labels.md`.

### Domain docs

Single-context: `CONTEXT.md` + `docs/adr/` en raíz. Ver `docs/agents/domain.md`.
